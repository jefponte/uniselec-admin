apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: uniselec-admin-stg
resources:
- ../../base
- namespace.yaml
patches:
- path: patch-deployment.yaml
- path: patch-ingress.yaml
- path: patch-hpa.yaml
configMapGenerator:
- behavior: merge
  literals:
  - LOG_LEVEL="info"
  - MESSAGE="Serviço Admin Staging"
  name: uniselec-admin-config
  options:
    disableNameSuffixHash: true
images:
- name: harbor.unilab.edu.br/unilab/uniselec-admin
  newTag: stg-2f5103f0
